#include<stdio.h>
int main()
{
    int n,t=0,rem,a,m;
    printf("enter the value of n");
    scanf("%d",&n);
    m=n;
    while(n>0)
    {
        rem=n%10;
        if(m%rem==0)
        {
            t++;
        }
        n=n/10;
        a=t;
    }
    printf("%d",a);
}
